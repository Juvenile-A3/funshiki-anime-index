# 首版验证记录

任务日期：2026-09-10。

## 数据

- 录播组 2026 年收藏夹当前最新三期：2026-06-20、2026-06-27、2026-07-04。
- 公共笔记专用正文接口 `/x/note/publish/info` 三期全部成功，无需登录。此前专栏正文接口的限流不影响这一实测结果，但未来仍需保留限流处理。
- 96 个原生时间标签，经验证相同标题、时间与视频版本后合并为 57 个话题。
- 21 个 Bangumi 动画条目、25 个已建立文本关联的话题，32 个其他或待确认话题保留在整期时间轴。
- Bangumi 搜索“至高指令”首位返回 OAD，首批人工选择 2016 TV 条目 149959，避免直接取首个搜索结果。
- 本轮未逐段听看录播；播放器实际跳转在不同 B 站客户端的行为尚需实机验证。

## 自动检查

`npm run check` 包括 11 个 Node 测试、7 个 Python 测试和静态构建，全部通过。

覆盖：别名搜索、年份过滤、无结果、跨三期关联、普通/弹幕版本合并、分 P 与秒数、提前五秒钳制、失效 CID、未知动画、重复时点、文本转义、hash 路由、原生标签、PS 过滤、多 P 不猜测、补丁版本与原子校验。

构建仅复制白名单文件，dist 包含 index.html、app.mjs、domain.mjs、style.css、favicon.svg、catalog.json、config.json。不会发布历史调查原始文件、第三方源码、工具或凭据。

## 浏览器实测

- 首页桌面截图：导航、主视觉、搜索区、统计和卡片排版正常，Bangumi 封面加载成功。
- 搜索冰之城墙仅返回对应一部动画；点击进入详情，显示来自三期的三个时点。
- 勾选提前五秒，首个跳转链接 t=2520 变为 t=2515，分 P 未改变。
- 原始笔记链接和单条纠错入口正常。
- 审核草稿保存后计数由 0 变为 1，丢弃后恢复为 0；测试草稿已清理。
- 点击导出后页面生成成功反馈，但内置浏览器下载事件未被工具确认。为此增加可见、可复制的 JSON 补丁输出；该补充在最终浏览器连接中断后未再次做视觉检查。
- 手机视口 390×844，页面实际 clientWidth=375、scrollWidth=375，无横向溢出；两列卡片和筛选可用。图像加载完成后可读 naturalWidth>0。
- 搜索不存在番名时显示明确空状态，可清空筛选。
- 后续浏览器工具连接再次中断；GitHub 新建仓库页未成功打开。

## GitHub 状态

本地 Git 仓库已初始化。官方 GitHub CLI 便携版在项目内，仅作本机工具且被 .gitignore 排除；auth status 返回未登录。

GitHub Pages 发布工作流与手动候选采集工作流已准备。远端仓库尚未创建，尚未触发 GitHub Actions，也尚未生成公网地址。GitHub runner 上的 B 站访问能力待首次运行验证。
